{
  "name": "@stack-and-survive/leaderboard-api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --import tsx src/server.ts",
    "build": "node --import tsx scripts/build.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "start": "node dist/server.js",
    "test": "vitest run"
  },
  "dependencies": {
    "@stack-and-survive/cloud-domain": "workspace:*",
    "@stack-and-survive/scenarios": "workspace:*",
    "@stack-and-survive/schema": "workspace:*",
    "@stack-and-survive/simulation": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "22.19.15",
    "esbuild": "^0.25.0",
    "tsx": "^4.0.0"
  }
}
